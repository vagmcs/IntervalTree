branches:
  only:
    - master
    - release
    - develop
dist: bionic
language: scala
scala:
  - 2.12.10
  - 2.13.1
env:
  - JDK=openjdk11
  - JDK=oraclejdk11
before_script:
  - jdk_switcher use $JDK
script:
  - sbt "++${TRAVIS_SCALA_VERSION}" test
  - git diff --exit-code